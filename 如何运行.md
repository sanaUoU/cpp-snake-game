# 🎮 运行游戏

## 最简单的方法

打开终端，输入：

```bash
cd ~/Downloads/Documentation/SnakeGame
chmod +x compile.sh
./compile.sh
./SnakeGame
```

## 或者直接编译运行

```bash
cd ~/Downloads/Documentation/SnakeGame
g++ -std=c++20 main.cpp -o SnakeGame -I/usr/local/include -L/usr/local/lib -lsfml-graphics -lsfml-window -lsfml-system
./SnakeGame
```

## 在 Cursor 中打开

```bash
open -a Cursor ~/Downloads/Documentation/SnakeGame
```

或者直接把这个文件夹拖到 Cursor 图标上。

然后：
1. 打开 `main.cpp`
2. 按 `⌘+Shift+B`

游戏窗口会自动弹出！

---

**文件位置**：`~/Downloads/Documentation/SnakeGame`

**开始玩吧！🎮**
