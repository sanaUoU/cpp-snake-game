# ⚡ 超简单使用指南

## 🎯 只需 3 步

### 1️⃣ 打开终端

按 `⌘ + 空格`，输入 `Terminal`，回车

### 2️⃣ 进入项目文件夹

```bash
cd ~/Downloads/Documentation/SnakeGame
```

### 3️⃣ 选择你要做的事

**想直接玩游戏？**
```bash
./run.sh
```

**想打包成 .app 分享？**
```bash
./build_all.sh
```

**想简单编译测试？**
```bash
./compile.sh
./SnakeGame
```

---

## 🎮 就这么简单！

### 打包后的文件在哪？

```bash
open releases
```

### 想在 Cursor 中打开？

```bash
open -a Cursor .
```

然后按 `⌘+Shift+B` 运行

---

## 📁 重要文件

| 文件 | 用途 |
|------|------|
| `run.sh` | **快速玩游戏** |
| `build_all.sh` | **一键打包** |
| `build_mac.sh` | 只打包 Mac |
| `compile.sh` | 简单编译 |
| `main.cpp` | 源代码 |

---

## 💡 第一次使用？

运行任意脚本前，确保已安装 SFML：

```bash
brew install sfml
```

---

## 🐛 出错了？

**错误：permission denied**
```bash
chmod +x *.sh
```

**错误：找不到 SFML**
```bash
brew install sfml
```

**错误：找不到 g++**
```bash
xcode-select --install
```

---

## 🎁 分享给朋友

打包后发送：
- Mac: `releases/mac/SnakeGame-Mac.zip`
- Windows: 需要在 Windows 电脑上打包

---

**就是这么简单！🚀**
